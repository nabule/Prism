# 开发运行说明

## 当前阶段

当前实现范围为 P0+P1，并开始 P2 的本地标签整理与 AI 整理 memo 草案：

- FastAPI Sidecar API。
- Memos webhook 接收。
- SQLite 任务表、工作区表和 memo 映射表。
- 后台 worker 轮询处理 `process_memo` 任务。
- Memos API Client 基础封装。
- Memos 用户 webhook 自动配置探针。
- 标签树配置加载与本地整理草案生成。
- 候选标签持久化与后台审核接口。
- AI 整理 memo 本地模板创建，并在 Sidecar 数据库记录来源关联。
- 待澄清 memo 评论追问，并把任务置为 `waiting_user`。
- OpenAI-compatible LLM provider 接入，默认使用 OpenRouter 免费模型配置。

附件解析、RAG、自动文档和备份恢复属于后续阶段。

## 环境准备

本项目在 Linux 环境下开发和测试。所有常用开发命令通过 Nx 任务入口执行：

```bash
npx nx test sidecar
npx nx run sidecar:compile
npx nx build sidecar
```

不要直接运行 `pip install`；Python 包和虚拟环境统一由 `uv` 管理，并封装在 Nx target、容器构建或后续统一脚本中。开发阶段优先执行本地 Nx 测试，Docker 构建放到阶段末尾验收。

## 启动 API

```bash
npx nx run sidecar:up
```

健康检查：

```bash
curl http://localhost:8080/health
```

## Memos 探针

执行 Memos API 探针：

```bash
npx nx run sidecar:probe-memos
```

首次本地联调可创建测试用户、登录并创建 memo：

```bash
npx nx run sidecar:probe-memos -- --bootstrap-username test --bootstrap-password testtest --create-memo "探针 memo #系统/原始记录"
```

创建 Memos Personal Access Token，供 Sidecar/worker 长期读取 Memos API：

```bash
npx nx run sidecar:probe-memos -- \
  --bootstrap-username test \
  --bootstrap-password testtest \
  --create-pat \
  --pat-output-env-file /tmp/memosima-pat.env
```

默认输出会隐藏 PAT 原文；`/tmp/memosima-pat.env` 仅供本机临时注入容器，不要提交到仓库。

配置 Memos 内置 webhook：

```bash
npx nx run sidecar:probe-memos -- \
  --bootstrap-username test \
  --bootstrap-password testtest \
  --configure-webhook-url "https://your-public-sidecar.example.com/webhooks/memos"
```

也可以通过 `.env` 配置 `MEMOS_WEBHOOK_URL`，然后直接执行探针。Memos `0.28.0` 会拒绝 webhook URL 解析到本机、Docker 内网或其他保留/私有 IP，例如 `http://sidecar:8080/webhooks/memos`、`http://localhost:8080/webhooks/memos` 和 `http://172.x.x.x:8080/webhooks/memos`。真实自动回调验收需要公网可达 URL，或使用公网隧道把 `http://localhost:8080` 暴露出去。

配置 webhook、创建 memo 并轮询 Sidecar 任务状态：

```bash
npx nx run sidecar:probe-memos -- \
  --bootstrap-username test \
  --bootstrap-password testtest \
  --configure-webhook-url "$MEMOS_WEBHOOK_URL" \
  --create-memo "自动 webhook 探针 #系统/原始记录" \
  --verify-sidecar-url "http://localhost:8080" \
  --verify-sidecar-token "$SIDECAR_ADMIN_TOKEN"
```

## 启动 Worker

持续轮询由 Compose 中的 `sidecar-worker` 服务负责：

```bash
npx nx run sidecar:up
```

## 管理接口

查询任务：

```bash
curl -H "Authorization: Bearer $SIDECAR_ADMIN_TOKEN" \
  "http://localhost:8080/admin/jobs"
```

重试失败任务：

```bash
curl -X POST -H "Authorization: Bearer $SIDECAR_ADMIN_TOKEN" \
  "http://localhost:8080/admin/jobs/1/retry"
```

## 测试

```bash
npx nx test sidecar
```

当前已通过自动化测试：

```text
npx nx test sidecar
26 passed
```

## P2 本地整理草案

worker 处理 memo 时会加载 `config/taxonomy.yaml`，在 job `result.ai_plan` 中写入本地整理草案：

- `system_tags`：默认包含 `#系统/原始记录`，必要时包含 `#系统/待澄清` 或 `#系统/标签待审核`。
- `active_tags`：命中已审核业务标签或别名后的正式标签。
- `candidate_tags`：memo 中出现但不在已审核标签树中的业务标签。
- `disabled_tags`：命中禁用标签的条目。
- `needs_clarification`：短内容或包含问号时标记为需要澄清。

该草案用于 P2 早期验证标签治理和任务结构。内容明确时，worker 会创建一条 Markdown AI 整理 memo，内容包含 `#系统/AI整理`、来源 memo、摘要、标签状态和原文摘录，并在 job `result.ai_summary_memo_uid` 返回新 memo uid。

当 `OPENROUTER_API_KEY` 存在时，worker 会按 `config/models.yaml` 调用 OpenAI-compatible `/chat/completions`，要求模型返回 JSON 对象，并把模型输出的摘要、要点和待办写入 AI 整理 memo。当前默认模型为 `deepseek/deepseek-v4-flash:free`。如果未配置模型 key，worker 会自动回退到本地模板整理，job `result.ai_source` 会标记为 `local`；使用 LLM 时标记为 `llm`。

Sidecar 会在本地 `memos` 表记录两类 memo：

- `type=original`：原始 Memos memo，同步状态为 `synced`。
- `type=ai_summary`：AI 整理 memo，`source_memo_uid` 指向原始 memo，状态为 `created`。

当前关联先保存在 Sidecar 数据库中；写入 Memos 原生 relation API 会在完成接口格式探针后再接入。

当 `needs_clarification=true` 时，worker 不会创建 AI 整理 memo，而是在原 memo 下创建澄清评论，并把任务状态置为 `waiting_user`。补充原始 memo 内容后，可以通过现有重试接口重新处理：

```bash
curl -X POST -H "Authorization: Bearer $SIDECAR_ADMIN_TOKEN" \
  "http://localhost:8080/admin/jobs/1/retry"
```

候选标签管理接口：

```bash
curl -H "Authorization: Bearer $SIDECAR_ADMIN_TOKEN" \
  "http://localhost:8080/admin/tag-candidates"

curl -X POST -H "Authorization: Bearer $SIDECAR_ADMIN_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"note":"纳入正式标签"}' \
  "http://localhost:8080/admin/tag-candidates/1/approve"

curl -X POST -H "Authorization: Bearer $SIDECAR_ADMIN_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"note":"重复标签"}' \
  "http://localhost:8080/admin/tag-candidates/1/reject"
```

当前接口只更新 Sidecar 数据库中的候选标签状态；把审核通过的标签写回 `config/taxonomy.yaml` 或后续标签表，会在下一阶段实现。

## Docker 状态

仓库已提供 `docker-compose.yml` 和 `Dockerfile` 草案。Docker Hub 镜像下载优先使用 Xget 加速地址，例如 `xget.your-domain.com/cr/docker/neosmemo/memos:stable`。

最近一次 `docker compose pull memos` 与 `npx nx build sidecar` 已通过 `xget.your-domain.com` 成功完成镜像拉取和构建。真实 Memos 手动 webhook 联调已通过；Memos 内置 webhook 自动配置接口已验证，但自动回调需要公网可达的 Sidecar webhook URL。

Dockerfile 已将第三方依赖安装与项目源码拷贝分层，后续普通代码变更不会反复下载全部 Python 依赖。
