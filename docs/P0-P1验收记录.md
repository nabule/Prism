# P0-P1 验收记录

## 已完成

- 创建 Python 项目骨架。
- 添加 FastAPI API 服务。
- 添加 SQLite 数据库迁移和任务存储。
- 添加 Memos Client 基础封装。
- 添加 webhook 幂等键生成。
- 添加后台 worker 单任务处理。
- 添加 Docker Compose 和 Dockerfile 草案。
- 添加 OpenRouter 默认模型配置：`deepseek/deepseek-v4-flash:free`。
- 添加开发运行、配置和 API 探针文档。

## 自动化测试

执行命令：

```bash
npx nx test sidecar
```

结果：

```text
13 passed
```

覆盖场景：

- OpenRouter 模型配置加载。
- 环境变量读取。
- 健康检查不泄漏真实 key。
- webhook memo UID 提取。
- webhook 幂等键生成。
- webhook 重放不重复创建任务。
- 管理接口 token 鉴权。
- jobs 查询。
- 任务 claim、failed、retry 状态流转。
- worker 使用模拟 Memos Client 处理 memo。
- Memos Client 登录、创建用户、创建 memo、读取 memo、创建评论。

## 未完成的真实验收

镜像拉取阻塞已通过切换 Xget 域名解决：

```text
Docker Hub: failed to resolve reference "docker.io/neosmemo/memos:stable" / i/o timeout
xget.xi-xu.me Docker Hub mirror: unexpected status 429 Too Many Requests
xget.xi-xu.me GHCR mirror: unexpected status 429 Too Many Requests
xget.your-domain.com Docker Hub mirror: docker compose pull memos 成功
xget.your-domain.com GHCR mirror: npx nx build sidecar 成功
```

已按 Xget 规则将 Docker Hub/GHCR 镜像改为 `xget.your-domain.com/cr/...` 加速地址，后续需要继续执行 Compose 启动与真实 webhook 联调。

## 真实 Memos 联调记录（2026-05-19）

已完成：

- `npx nx run sidecar:up` 启动 Memos、Sidecar、worker。
- Memos 版本：`0.28.0`，schema：`0.28.1`。
- Sidecar 健康检查通过：`GET http://localhost:8080/health`。
- 通过 Memos API 创建首个管理员用户。
- 通过 `POST /api/v1/auth/signin` 获取 access token。
- 通过 `POST /api/v1/memos` 创建真实 memo：`memos/LH9yyJTHf5Uz7i958AG8p7`。
- `npx nx run sidecar:probe-memos -- --bootstrap-username test --bootstrap-password testtest --create-memo ...` 可完成登录、创建 memo、读取 memo 的 Memos API 探针。
- 向 `POST /webhooks/memos` 投递真实 memo webhook。
- worker 使用真实 Memos API 读取 memo，任务状态变为 `succeeded`。
- Sidecar `memos` 表写入原始 memo 映射，状态为 `synced`。

仍未完成：

- 尚未配置 Memos 内置 webhook 自动回调到 Sidecar；本次 webhook 为手动投递真实 payload。
- 探针评论默认关闭，未在本轮写入评论。

在 Docker 可用或提供远程 Memos 实例后，需要补跑：

```text
手动创建 3 条 Memos memo
-> webhook 到达 Sidecar
-> 产生 3 个 process_memo 任务
-> worker 拉取真实 memo 详情
-> 全部任务成功
-> 重放同一 webhook 不产生重复任务
```

## 安全记录

用户提供的 OpenRouter key 未写入仓库文件。仓库只包含 `OPENROUTER_API_KEY` 环境变量占位。
