# 配置说明

## 环境变量

复制示例文件：

```bash
cp .env.example .env
```

必须配置：

| 变量 | 说明 |
| --- | --- |
| `SIDECAR_ADMIN_TOKEN` | 管理接口 Bearer token |
| `MEMOS_BASE_URL` | Memos 服务地址 |
| `MEMOS_API_TOKEN` | Memos API token |
| `MEMOS_WEBHOOK_URL` | Memos 回调 Sidecar 的公网 URL，例如 `https://your-public-sidecar.example.com/webhooks/memos` |
| `OPENROUTER_API_KEY` | OpenRouter API key |

`OPENROUTER_API_KEY` 只通过环境变量读取，不写入仓库。不要把真实 key 提交到 git。

`MEMOS_API_TOKEN` 建议使用 Memos Personal Access Token，而不是登录接口返回的短期 access token。可通过 Nx 探针生成：

```bash
npx nx run sidecar:probe-memos -- \
  --bootstrap-username test \
  --bootstrap-password testtest \
  --create-pat \
  --pat-output-env-file /tmp/memosima-pat.env
```

探针默认不会在 JSON 输出中显示 PAT 原文；临时 env 文件只用于本机注入容器或复制到受控密钥管理位置。

## app.yaml

`config/app.yaml` 控制 Sidecar 基础行为：

- `database.path`：Sidecar SQLite 路径。
- `security.admin_token_env`：管理 token 的环境变量名。
- `memos.base_url_env`：Memos 地址的环境变量名。
- `memos.api_token_env`：Memos token 的环境变量名。
- `memos.webhook_url_env`：Memos 内置 webhook 回调 URL 的环境变量名。
- `worker.max_attempts`：任务失败后的最大尝试次数。
- `worker.create_probe_comment`：P0 探针是否在 memo 下写入测试评论，默认关闭。

Memos `0.28.0` 会拒绝 webhook URL 指向本机、Docker 内网或其他保留/私有 IP。Compose 内部地址 `http://sidecar:8080/webhooks/memos` 不能用于 Memos 内置 webhook；需要配置公网可达 URL，或使用公网隧道转发到本机 Sidecar。

## models.yaml

当前默认模型配置：

```yaml
default_provider: openrouter
providers:
  openrouter:
    base_url: https://openrouter.ai/api/v1
    api_key_env: OPENROUTER_API_KEY
    default_model: deepseek/deepseek-v4-flash:free
```

worker 会读取默认 provider，并在对应 `api_key_env` 环境变量存在时发起 OpenAI-compatible `/chat/completions` 调用。模型输出必须是 JSON 对象，Sidecar 会校验 `title`、`summary`、`key_points`、`todos`、`needs_clarification` 和 `clarification_question` 字段。没有配置 key 时不调用 LLM，自动回退到本地模板整理。

## taxonomy.yaml

`config/taxonomy.yaml` 保存系统标签、初始业务标签、别名和禁用标签。路径由 `config/app.yaml` 中的 `taxonomy.path` 指定。

当前 P2 本地整理草案会读取该配置：

- `system_tags`：用于标识原始记录、AI 整理、待澄清、标签待审核等状态。
- `business_tags`：已审核业务标签；只有 `status: active` 的标签会被直接使用。
- `aliases`：把常用别名映射到正式业务标签。
- `disabled`：禁用标签，命中后不会进入正式标签或候选标签。

worker 会把解析结果写入 job `result.ai_plan`。内容明确时会用 `system_tags.ai_summary` 创建 AI 整理 memo；内容过短或包含问号时会用 `system_tags.pending_clarification` 标记草案，在原 memo 下写澄清评论，并把任务置为 `waiting_user`。

候选标签持久化后会进入 Sidecar SQLite 的 `tag_candidates` 表，可通过 `/admin/tag-candidates` 查询，并通过 `/admin/tag-candidates/{id}/approve` 或 `/admin/tag-candidates/{id}/reject` 审核。当前审核结果只保存在数据库，不会自动改写 `config/taxonomy.yaml`。
